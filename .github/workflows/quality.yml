name: Required quality

on:
  pull_request:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  quality:
    uses: cpeter1207/rptadv-samplerate-adapter-workflows/.github/workflows/quality.yml@main
    with:
      code_ref: ${{ github.sha }}
